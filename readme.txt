First: This Version is working. Current Updates are done to aid maintainability and to document what the given code segment is supposed to do, as well as to streamline the used syntax in any given segment, to further improve readability.

Instructions:
Before launching the App, you need to set your jwtPrivateKey as an env variable (name: "jwtPrivateKey") as well as the db address to the mongodb database you want to use(config/default.json - "db"). If you want to run the tests, you need to adjust the "db" in config/test.json as well

Use:
This App is suposed to shwocase CRUD operations initialised by RESTfull services
